server {
  listen   80;
  server_name  <%= node['hostname'] %>;

  access_log  <%= node['nginx']['log_dir'] %>/localhost.access.log;

  location / {
      proxy_pass http://127.0.0.1:8000;
      proxy_set_header X-Forwarded-Host <%= node['hostname'] %>;
  }
}
